#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - task 2
 * Description: return alphabet in small case
 * Return: 0
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		putchar(i + '0');
	putchar('\n');
	return (0);
}
