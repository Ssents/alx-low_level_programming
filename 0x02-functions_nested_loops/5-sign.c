#include "main.h"
/**
 * print_sign - checks the value of the input
 * @n: character input for fucntion
 * Return: 0 or 1
 */
int print_sign(int n)
{

	if (n > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
